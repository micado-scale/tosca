# This ADT will be generated by a program (not yet implemented) See:
# "[This prototype] requires the creation of a small program
# which generates a TOSCA profile from different files
# associated with different applications..."